schema: spec-driven

context: |
  Stack: Astro 7.0.9 (static output), Tailwind CSS 4 via @tailwindcss/vite, Alpine.js 3.
  Extras: Swiper 12, @lucide/astro, Sharp, Zod, schema-dts. Package manager: pnpm only
  (npm/npx forbidden, enforced by scripts/enforce-package-manager.cjs + .npmrc + devEngines).
  Architecture: multipage Astro site (src/pages/*.astro), layout components in
  src/components/layout/, section components in src/components/sections/, SEO components in
  src/components/seo/, UI primitives in src/components/ui/.
  Data contract: 12 typed JSON files under src/data/*.json with Zod validation
  (business, site, navigation, hero, services, gallery, testimonials, faq, areas,
  directories, blog, landings). Preserve schema per AGENTS.md.
  Testing: no test runner configured; strict_tdd disabled.

strict_tdd: false

testing:
  strict_tdd: false
  detected: 2026-07-15
  test_runner:
    command: ""
    framework: none
  layers:
    unit: false
    integration: false
    e2e: false
  coverage:
    available: false
    command: ""
  quality:
    linter:
      available: false
      command: ""
    type_checker:
      available: true
      command: "pnpm exec astro check"
    formatter:
      available: false
      command: ""
  notes: >
    No unit/integration/e2e runner is installed (no vitest, jest, playwright,
    or cypress in package.json; no test files under src/). `astro check`
    provides type/template diagnostics via the Astro + TS toolchain
    (astro.config.mjs, tsconfig.json) but is not a test suite.

rules:
  proposal:
    - Use pnpm-only commands in any proposed workflow; npm/npx is forbidden (AGENTS.md).
    - Preserve the template's neutral/placeholder-only content unless the task explicitly targets a client build.
    - Include rollback plan for risky changes.
  specs:
    - Use Given/When/Then for scenarios.
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY).
    - Treat the 12 src/data/*.json files as a stable contract: preserve top-level keys, nested object shape, required arrays, and _instructions blocks.
  design:
    - Document impact on the JSON data contract explicitly (additive vs shape-breaking).
    - Note any change to pnpm/.npmrc/pnpm-workspace.yaml enforcement files as high-risk.
  tasks:
    - Group by phase, use hierarchical numbering.
    - Keep tasks completable in one session.
  apply:
    rules:
      - Follow existing Astro component patterns (layout/ vs sections/ split).
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "pnpm run build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas to the JSON data contract.
